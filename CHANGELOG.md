# 更新日志

本项目的所有重要变更都会记录在这里，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- 新增命令行工具 **`--uploadall <令牌或分享链接> <本地文件夹>`**：把本地文件夹里的音频与 `.lrc` 一次性补齐到云端（云端没有的上传、同名同大小跳过、同名不同大小覆盖），传完会打印清单。适合「先把本地全部传上去，确认无误后删本地」的用法。
- `--cloudtest` 升级为**整库校验**：逐首拉取文件头解析时长，并统计歌词覆盖率，异常文件会列出来（本次用它确认了 27 首全部可解析）。

### 说明

- 用上面的流程把本地 10 首歌 + 歌词 + 一个 64MB 无损文件（约 156MB）传到云盘，**逐文件 MD5 与云端一致**后才清理了本地副本。

## [1.5.0] - 2026-09-16

### 新增

- **播放列表会延续了**：队列与当前曲目、播放进度在每次变化后即时保存，下次打开自动恢复成上次的列表（之前会被「空队列」覆盖，所以每次都是 0 首）。
- 桌面歌词的「解锁」按钮改为**鼠标靠近才出现**，移开约 1.4 秒后自动隐藏（之前一直挂在屏幕上）。
- 桌面歌词排版按有无译文区分：
  - 当前句**有译文**（外语歌常见）：只显示这一句，原文在上、译文在下，**字号与字重相同**，不再挤一行下一句预览；
  - 当前句**没有译文**：仍然显示下一句作为预览。

### 变更

- 歌词页里译文行字号由 12.5 调到 14，外语歌看着更清楚。

## [1.4.1] - 2026-09-16

### 修复

- **只填 API 令牌、分享链接留空也能正常使用**（已实测：列目录、播放、缓存、歌词全部正常）。
- 侧边栏左下角不再显示令牌明文，令牌模式下显示资料库名称（如「云盘（API 令牌）：music」）。
- 令牌模式下右上角「打开云盘」改为打开资料库网页，不再拼出无效的分享链接地址。
- 分享链接为空时点「测试连接」会提示改用「检测令牌」，不再报无意义的错误。

## [1.4.0] - 2026-09-16

### 新增

- **支持「资料库 API 令牌」**（网页版：资料库 → 设置 → API 令牌，权限选读写）：填了令牌就优先直连资料库，不再依赖分享链接，并且可以
  - **直接从播放器删除云端文件**（音乐库右键 → 「从云盘删除…」，会二次确认，文件进云盘回收站）；
  - 上传时自动覆盖同名文件；
  - 在设置页点「检测令牌」查看资料库名称与文件总数。
- 令牌与分享链接使用同一个「云端音乐」卡片配置，填哪个都能用。

### 说明

- 令牌等同密码，只保存在本机 `%APPDATA%\LightMusic\settings.json` 里，不会上传、也不会写进仓库。

## [1.3.0] - 2026-09-16

### 新增

- **上传到云盘**：音乐库顶部「上传歌曲」、设置页「上传歌曲」，或直接把歌曲 / 歌词文件（也可以整个文件夹）拖进窗口即可上传到云盘分享目录；侧边栏显示上传进度，完成后自动刷新列表。
- **歌词颜色可自定义**：设置页提供 10 个预设颜色（新增黑色 / 深灰 / 深蓝 / 深红，浅色背景也能看清）以及「自定义…」取色器；桌面歌词右键菜单也能直接换色。
- **锁定时的解锁按钮**：锁定桌面歌词后，右上角会常驻一个「解锁」小按钮（独立窗口，锁定时依然可点），用鼠标就能解锁；其余按钮只在未锁定时出现。

### 变更

- 歌词页改用 ScrollViewer 承载歌词并做平滑滚动，修复长歌词只能显示前面若干行、滚到后面一片空白的问题；手动滚动 6 秒后自动回到当前歌词。

### 修复

- 修复桌面歌词拖动时与悬停检测冲突可能导致的卡住。
- 修复音乐库改为单选后右键点击报错的问题。
- 修复歌词页上下淡出遮罩在切换主题后颜色不更新的问题。
- 修复设置页「缓存占用」文字与「清理缓存」按钮没有垂直居中的问题。

## [1.2.0] - 2026-09-16

### 新增

- **云端音乐库**：支持把清华云盘（Seafile）分享链接当作音乐库，读取目录、时长与歌词，播放时自动下载到本地缓存。
- 自动预取播放列表里的下一首云端歌曲；缓冲时在播放条底部显示进度。
- 云端缓存管理：显示占用、一键清理，可关闭缓存（播完即删）。
- 新增命令行自检：`--cloudtest <分享链接>` 校验列目录 / Range 解析时长 / 下载 / 歌词解析，`--streamtest` 校验流式播放。

### 变更

- **去掉本地文件夹作为音乐来源**，播放器改为纯云端模式（桌面歌词、播放列表等功能不变）。
- **点击歌名 = 直接播放这一首 + 把它加入播放列表**，不再需要单独的「＋」按钮；重复点击同一首不会重复加入。
- 「下一首播放」如果歌曲已在列表里，会把它移动到下一首的位置，而不是重复插入。

### 修复

- 修复关闭窗口时可能卡住的问题（不再在关闭流程里调用 UnregisterHotKey）。

## [1.1.0] - 2026-09-16

### 新增

- 主题新增「跟随系统」并作为默认选项，系统在浅色 / 深色之间切换时播放器会自动跟随。
- 音乐库顶部新增「播放全部」与「随机播放」按钮。
- 音乐库每行右侧新增 **＋** 按钮：把这一首单独加入播放队列；单击歌名 / 整行即直接播放。
- 播放队列每行右侧新增 **✕** 按钮：逐首移出队列。
- 桌面歌词右上角新增悬停工具栏：锁定 / 解锁、字号加减、回到主界面、关闭，锁定状态下也能用鼠标操作。

### 变更

- 重新设计歌词页：左侧唱片信息卡片（含歌词偏移与桌面歌词开关），右侧逐行歌词按与当前句的距离渐隐、上下淡出遮罩、可直接点句跳转，手动滚动后出现「回到当前歌词」。
- 重新设计设置页为双列卡片布局，音乐库与关于整行展示，右侧不再留白。
- 桌面歌词宽度固定不再跳变；锁定时完全没有背景与阴影，未锁定时鼠标移上去才显示底色与阴影。
- 去掉音乐库序号悬停变成播放键的行为，序号列只用于显示序号与当前播放高亮。

### 修复

- 修复进度条与设置页的滑块：圆形滑点与已播放（已填充）色条的端点现在严丝合缝对齐。

## [1.0.0] - 2026-09-16

第一个可用版本。

### 新增

- 音乐库：扫描本地音乐文件夹，按「歌名 - 歌手」解析歌名与歌手（支持多歌手），支持搜索与按序号 / 歌曲 / 歌手 / 时长排序。
- 播放控制：播放 / 暂停、上一首 / 下一首、进度拖动、音量与静音。
- 播放模式：顺序播放、列表循环、单曲循环、随机播放。
- 播放队列：从音乐库双击播放、下一首播放、添加到队列、移除、上移 / 下移、清空，支持 M3U 导入导出。
- 歌词：软件内歌词页（逐行高亮、自动滚动、点击跳转、翻译行、偏移微调）与桌面歌词浮窗。
- 桌面歌词：无边框置顶、可拖动、可调字号 / 颜色 / 不透明度；**支持锁定（鼠标穿透）**，锁定时不抢焦点、不占 Alt+Tab。
- 音乐目录：默认自动探测 `music` 文件夹，可在设置中更改，也支持把文件夹拖进窗口。
- 外观与体验：深色 / 浅色主题、矢量图标、托盘图标、全局多媒体键、记忆播放进度与窗口位置、单实例运行。
- 工程：`build.ps1` 一键构建（系统自带 csc.exe，产物为单文件 exe）、`scripts\install.ps1` 创建桌面快捷方式、无界面自检 `--selftest`、离屏截图 `--shot`、冒烟测试 `--smoke`、鼠标穿透校验 `--lockcheck`。
