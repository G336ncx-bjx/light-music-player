# 更新日志

本项目的所有重要变更都会记录在这里，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.2.0] - 2026-09-16

### 新增

- **云端音乐库**：支持把清华云盘（Seafile）分享链接当作音乐库，读取目录、时长与歌词，播放时自动下载到本地缓存。
- 自动预取播放列表里的下一首云端歌曲；缓冲时在播放条底部显示进度。
- 云端缓存管理：显示占用、一键清理，可关闭缓存（播完即删）。
- 新增命令行自检：`--cloudtest <分享链接>` 校验列目录 / Range 解析时长 / 下载 / 歌词解析，`--streamtest` 校验流式播放。

### 变更

- **去掉本地文件夹作为音乐来源**，播放器改为纯云端模式（桌面歌词、播放列表等功能不变）。
- **点击歌名 = 直接播放这一首 + 把它加入播放列表**，不再需要单独的「＋」按钮；重复点击同一首不会重复加入。
- 「下一首播放」如果歌曲已在列表里，会把它移动到下一首的位置，而不是重复插入。

### 修复

- 修复关闭窗口时可能卡住的问题（不再在关闭流程里调用 UnregisterHotKey）。

## [1.1.0] - 2026-09-16

### 新增

- 主题新增「跟随系统」并作为默认选项，系统在浅色 / 深色之间切换时播放器会自动跟随。
- 音乐库顶部新增「播放全部」与「随机播放」按钮。
- 音乐库每行右侧新增 **＋** 按钮：把这一首单独加入播放队列；单击歌名 / 整行即直接播放。
- 播放队列每行右侧新增 **✕** 按钮：逐首移出队列。
- 桌面歌词右上角新增悬停工具栏：锁定 / 解锁、字号加减、回到主界面、关闭，锁定状态下也能用鼠标操作。

### 变更

- 重新设计歌词页：左侧唱片信息卡片（含歌词偏移与桌面歌词开关），右侧逐行歌词按与当前句的距离渐隐、上下淡出遮罩、可直接点句跳转，手动滚动后出现「回到当前歌词」。
- 重新设计设置页为双列卡片布局，音乐库与关于整行展示，右侧不再留白。
- 桌面歌词宽度固定不再跳变；锁定时完全没有背景与阴影，未锁定时鼠标移上去才显示底色与阴影。
- 去掉音乐库序号悬停变成播放键的行为，序号列只用于显示序号与当前播放高亮。

### 修复

- 修复进度条与设置页的滑块：圆形滑点与已播放（已填充）色条的端点现在严丝合缝对齐。

## [1.0.0] - 2026-09-16

第一个可用版本。

### 新增

- 音乐库：扫描本地音乐文件夹，按「歌名 - 歌手」解析歌名与歌手（支持多歌手），支持搜索与按序号 / 歌曲 / 歌手 / 时长排序。
- 播放控制：播放 / 暂停、上一首 / 下一首、进度拖动、音量与静音。
- 播放模式：顺序播放、列表循环、单曲循环、随机播放。
- 播放队列：从音乐库双击播放、下一首播放、添加到队列、移除、上移 / 下移、清空，支持 M3U 导入导出。
- 歌词：软件内歌词页（逐行高亮、自动滚动、点击跳转、翻译行、偏移微调）与桌面歌词浮窗。
- 桌面歌词：无边框置顶、可拖动、可调字号 / 颜色 / 不透明度；**支持锁定（鼠标穿透）**，锁定时不抢焦点、不占 Alt+Tab。
- 音乐目录：默认自动探测 `music` 文件夹，可在设置中更改，也支持把文件夹拖进窗口。
- 外观与体验：深色 / 浅色主题、矢量图标、托盘图标、全局多媒体键、记忆播放进度与窗口位置、单实例运行。
- 工程：`build.ps1` 一键构建（系统自带 csc.exe，产物为单文件 exe）、`scripts\install.ps1` 创建桌面快捷方式、无界面自检 `--selftest`、离屏截图 `--shot`、冒烟测试 `--smoke`、鼠标穿透校验 `--lockcheck`。
